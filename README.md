# ヲタ芸マスター

動画やインカメを、お手本と8カウントに合わせて比較するWebアプリ。HTML・CSS・JavaScriptと骨格モデルをGitHub Pagesで配信します。ChatGPTへのログイン、自前のアプリサーバー、APIキーは不要です。

## 使い方

1. お手本の動画ファイル、またはYouTubeの共有リンクを選びます。
2. 自分の動画ファイルを選ぶか、インカメを起動します。
3. 各動画の「設定 → 拍・BPM」でBPMを設定します。ローカル動画2本なら、下の「1拍目」で両方の映像を同じ「1」の瞬間に合わせ、「この2点を保存」して「同期再生」を押します。
4. ローカル動画同士なら「重ねる → 位置 → 動画全体で自動補正」で対象の2人を確認し、「この2人で解析」を押します。BPMと拍の位置を先に合わせてください。インカメや1コマだけの補正も使えます。
5. 次回は「履歴」から保存済みの動画・URLを読み直せます。

## 拍タップと白い拍線

「1拍目」画面では、お手本と自分をそれぞれ1拍単位・0.02秒単位・秒数の直接入力で動かせます。「2拍前から拍音で確認」で両方を同じ拍の位置から再生し、「この2点を保存」で端末に保存します。「1拍目から再生」は、その2点から始めます。動画の先頭が近く2拍分戻れないときは、両方で同じ拍数だけ戻ります。設定時は古いループをOFFにし、切り抜き前の元映像で位置を確認します。

タップ間隔だけでなく、タップした動画の再生時刻からBPMと拍の位置を同時に推定します。最初にタップした拍を「1」として数えますが、その最初の時刻にも固定せず、有効なタップ全体から等間隔の拍線を当てはめます。大きく外れたタップや二重タップの影響を抑え、抜けた拍を推定します。上段の点が実際のタップ、白線と下段の点が補正後の拍です。白い拍線・8カウント・2本の動画の同期に同じBPMと起点を使います。

64回の入力は不要です。2回から暫定推定を始め、有効な結果をこの画面に保持します。「拍音で確認」で聴いてから「保存して戻る」でこの端末に保存します。4〜16回程度が目安です。途中でタップをやめても、停止・画面切替・動画終了でも結果は残ります。再生位置変更や読み込み中断では記録を区切り、違う区間のタップを混ぜません。「再生してやり直す」で新しい記録を始められます。新しい推定が成立するまでは既存のBPM・起点を維持します。

少数のタップではテンポの倍・半分や押し間違いを区別できない場合があります。すべてのタップが一律に遅れる癖はタップだけでは判別できないため、必要なら「拍の位置」か下の矢印か「トラック」で微調整してください。テンポが途中で変わる曲は練習区間ごとに設定してください。

## 動画全体の自動サイズ補正

同期して比較できる区間全体から32組を解析します。最初に選んだ人物の腰位置と大きさに近い候補だけを使い、観客しか残らない場面や人物が曖昧な場面は除外します。対象を見失った場合は、見えている別人へ自動で切り替えません。大きく移動する動画や人が重なる動画では利用できない場合があります。

胴体の左右の長さを重み1、肩幅を重み0.5として、可視性で重み付けした長さ比の中央値で倍率を求めます。1.4倍を超えて外れる測定を除き、腰中心75%・足首中心25%で位置を決めます。足首が隠れている場合は腰だけを使います。仁王立ちは必須ではありません。全編で対象を確認できない場合、使える胴体の場面が6組未満の場合、補正が極端な場合は採用しません。

処理は端末内のWorkerで行い、モデルを1回ロードして再利用します。見えているプレイヤーとは別のミュートした動画から画像を読むため、解析中も再生位置・速度・音の設定は変わりません。途中で中止でき、動画の変更や画面サイズ変更でも処理を終了します。

補正後は1個の倍率と位置に固定し、手動の回転・遠近・背景の画角補正は維持します。重ねる位置はパネルを閉じたり縦横を切り替えても動画の表示サイズに合わせて換算します。位置・倍率の補正値はこの画面を開いている間だけ保持します。再読み込み後は補正をやり直してください。

## 今の1コマで骨格合わせ

「重ねる → 位置 → 今の1コマで合わせる」で実行します。自動補正は位置移動と縦横同じ倍率だけです。両方が立ち姿勢なら、頭から両足首の中点までの高さと足元で合わせます。

体の曲がり・片足の浮き・姿勢の違いがある場合は、見えている胴体の左右・太もも・すねの長さの比から概算します。極端な比を除いた中央値で倍率を決め、両腰の中点（腰が見えない場合は両肩の中点）で位置を合わせます。頭や足の一部が隠れていても、使える部位が残っていれば実行できます。概算したことは画面に表示します。

肩の傾き・足の開き幅から画面の回転や遠近は推定しません。手動で決めた回転・遠近は保持し、再生中は補正を固定します。自動倍率は0.5〜2倍、移動は±100%に抑え、大きな差は手動調整に残します。奥行き方向の短縮や体格差は分離できないため完全一致ではありません。肩・腰や十分な長さが検出できない場合だけ、場面変更や「位置・濃さ」での手動調整を案内します。

## 背景を基準に画角を補正

各動画の「設定 → 画角」で試せます。人体の姿勢・体格に合わせて画面を変形する代わりに、背景の固定した基準を使います。

1. 動画を止め、「水平線2点」または「壁の四隅」を選びます。番号をドラッグして基準点を合わせます。矢印キーでも微調整できます。
2. 水平線では背景の線の左端・右端を指定します。足先や奥へ続く床の線は使いません。これは選んだ線を水平にする操作であり、真のカメラ回転角の推定ではありません。
3. 四隅では人物に近い、人物と平行な壁・窓・鏡などの長方形を、左上→右上→右下→左下で指定します。実物の横÷縦を入力します（横100・縦200なら0.5）。実物が正方形のときだけ「正方形」を選びます。人体や床の四角には合わせません。
4. 「補正を試す」で元映像と比較し、必要なら強さを弱めます。「採用して保存」で練習画面にも反映されます。各動画で個別に設定でき、端末に保存します。
5. 採用・解除時は古い補正との二重適用を避けるため、重ねる位置・倍率・回転・手動遠近をリセットします。画角補正後に見比べたい姿勢で位置を合わせ直してください。

人物の奥行き、隠れた手足、広角レンズの曲がり、撮影中のカメラ移動は補正できません。動画ファイル向けで、YouTube・インカメは未対応。元のファイルやBPMは変えません。通常映像・切り抜き映像・骨格表示に同じ行列を適用します。[調査と合成実験の結果](docs/camera-correction-research.md)に方法の比較・数値・出典をまとめています。

## 背景を切り抜く

各動画の「設定 → 表示 → 背景」で、次の方式を選べます。比較・設定画面では緑背景または透明の市松模様で輪郭を確認でき、「重ねる」画面では背景を透明にしてお手本を透かします。自分側は「重ねる → 位置 → 背景」からも変更できます。

- **人物を自動で切り抜く**：普通の動画やインカメから、MediaPipe Selfie Segmenterで人物の領域を推定します。「輪郭の絞り込み」を上げると、確かな人物の領域だけを残します。遠くの全身、速い動き、髪や指、サイリウムは欠けたり背景が残ったりする場合があります。
- **撮影した緑背景を消す**：グリーンバックで撮影済みの映像の緑を透明にします。「緑を消す強さ」で調整します。緑の衣服やサイリウムも消えるため、撮影時の色に注意してください。

元の動画ファイルは変更せず、切り抜き表示に同じ反転・位置・倍率・遠近設定を適用します。音声とBPM同期は元の動画プレイヤーを使います。表示の画像処理はWorker内で行い、長辺640px・最大約30fps、処理待ちは1フレームまでに制限します。切り抜き画像の取得時刻も確認し、遅れが80ms＋元映像の1コマ相当（40ms）を超えた場合は元の映像に戻します。音声の時計が合っていても古い切り抜き映像だけが残ることを防ぎます。停止中の同じコマは保持し、背景をOFF→ONにすると再試行できます。

モデルはこのサイトから取得し、動画の画像を外部に送りません。Worker / OffscreenCanvas / ImageBitmapに対応するブラウザが必要です。処理失敗時は元の映像を表示します。YouTube埋め込みの切り抜き、切り抜き済み動画の書き出しは未対応です。「録画を保存」は背景処理前のカメラ映像です。背景の方式・強さ・確認用の色は、この端末に動画ごとに保存します。

## 各利用者の保存

- 動画のコピーとURL履歴は、その端末・ブラウザのIndexedDBに保存します。Cookieに動画を入れる方式ではありません。BPM・開始位置・反転設定は端末のlocalStorageに保存します。
- 動画をGitHubや運営者のサーバーへアップロードしません。各利用者のブラウザ内の履歴は、別の利用者の端末には共有されません。同じ端末・ブラウザを共用する人には同じ履歴が見えます。
- 別端末への同期、ログインアカウントごとのクラウド保存はありません。元の公開サイトとGitHub Pagesは別の保存領域なので、移行時は動画を一度選び直してください。
- 動画・URLを合わせて最近10件を保存します。使い直した項目は先頭へ移動し、11件目から古いアプリ内のコピーを入れ替えます。動画は合計1GB、1本500MBまで。容量制限や保存失敗では既存の履歴を保ち、今回だけの再生に使います。
- 「履歴に残す」のOFF、個別削除、全履歴の削除ができます。削除対象はアプリ内のコピーで、写真アプリ等の元動画は消しません。
- ブラウザのデータ削除、プライベートブラウズ、空き容量不足などで履歴が消える場合があります。原本は写真アプリ等にも残してください。「保存領域を保護」はブラウザへの保護リクエストであり、永久保存の保証ではありません。

## 外部サービスとの通信

YouTubeは公式の埋め込みプレイヤーを使います。リンクを読み込んだ際にYouTubeへ通信します。動画データの取得・ダウンロード・音声抽出は行いません。YouTubeも「重ねる」で自分の動画・インカメと半透明に重ねて表示できます。「位置」で自分の濃さ・倍率・上下左右・傾き・遠近を手動調整します。背景切り抜きはこの重ね表示では使わず、動画の読み直しや再生中の自動再同期も追加しません。「YouTubeを操作」で自分の映像だけを一時的に隠し、埋め込み内の操作後に「重ね表示に戻る」で戻せます。YouTubeの反転・骨格解析は未対応です。広告、通信、頭出しの精度による同期のずれが生じる場合があります。

曲名でのBPM検索はブラウザからDeezerの公開カタログへ直接問い合わせます。Apple Musicの曲リンクはiTunes lookupで曲名を取得してから検索します。検索した曲名や曲IDは各サービスへ送られます。音源・動画ファイルは送りません。BPMが未収録の場合は推測値で埋めません。サブスク音源の抽出や同期再生には対応しません。外部サービスが利用できない場合は、音源解析・手入力・TAP・rekordbox XMLを利用できます。

音声解析・動画の軽量化・骨格推定は端末内で行います。骨格モデルはこのサイトから初回読み込みます。補正は平面近似であり、3Dの体や隠れた手足を復元するものではありません。フォーム差は画角補正後、重ねる位置・倍率・手動遠近を適用する前の2D角度差で、残る撮影角度の影響を含みます。

## GitHub Pagesへの公開

このリポジトリの Settings → Pages → Build and deployment → Source を **GitHub Actions** に設定します。mainへのpushでテストと静的ビルドを実行し、`dist-pages`を公開します。プロジェクトURLのサブディレクトリでも動く相対パスを使用しています。

GitHubが静的ファイルの配信を担当するため、PCや自分のサーバーを常時起動する必要はありません。YouTube・曲検索・初回のアプリ読み込みにはネット接続が必要です。完全オフライン動作を保証するService Workerは未実装です。iPhoneではSafariの共有からホーム画面に追加できます。

## 開発と検証

Node.js 24:

```sh
npm ci
npm test
npm run check
npm run build
node tests/built-worker.mjs
node tests/built-background-worker.mjs
node tests/static-output.mjs
```

`npm run dev` はUIの開発用です。MediaPipeが必要とするclassic Workerは開発サーバーでは未バンドルのimportを実行できないため、背景処理・動画全体の骨格解析を確認するときは `npm run build` の後に `npm run preview` を使ってください。公開先はビルド済みのWorkerを配信します。

保存・再読込・削除・容量制限はfake-indexeddbで検証します。音声解析は合成音のテストです。背景処理は合成画素・マスクと、ビルド済みWorkerの緑除去経路をメモリ上の2D描画代替で検証します。AIモデルの実映像での切り抜き精度・ブラウザ内のWorker動作・Safari実機の保存維持、実際の楽曲の解析精度、YouTubeの再生・同期、カメラ、動画デコード、骨格推定・遠近表示は実機での追加確認が必要です。

第三者ライブラリ・モデルのライセンスは依存パッケージと`public/`内のLICENSE・NOTICE類を参照してください。

## 音の基準と拍音プレビュー

2本のトラック上部の「曲」で「お手本／自分」を選び、右端のドロップダウンで0.25〜2.5倍を0.25刻みで変更します。細かな倍率は、下部の「速度・音」の数値入力・スライダーで調整できます。選んだ曲のBPMを基準に、もう片方の速度を合わせます。例：お手本150 BPM・自分120 BPMなら、自分の音を選び練習速度1倍にすると、お手本0.8倍・自分1倍になります。YouTubeでも主役に「自分」を選ぶと練習速度1倍を優先し、YouTube側の倍率を再生開始時に確認します。必要な倍率が通れば自分の音を1倍に保ち、丸められる場合は0.05刻みの最も近い倍率を試します。それも使えない場合は公開された速度候補から最も近い組み合わせを自動選択します。確認したYouTubeの倍率に合わせて自分側も調整し、頭出しは開始時の1回です。再生中の定期的な速度変更や再同期は行いません。インカメは音声を取得しません。

各動画の「設定 → 拍・BPM／拍の位置」の「拍音で確認」は、最初の「1」の2拍前（動画の範囲内）からその動画だけを等速・音声付きで再生します。白線が設定した拍、赤線が再生位置です。白線のタイミングでクリックを鳴らし、各エイトの「1」「5」は同じ高い音にします。停止・移動・終了で予約した拍音を取り消します。確認中の結果は「保存して戻る」を押すまで永続保存しません。

再生中はBPMの比率で決めた倍率を固定し、時計の差を理由に停止・シーク・速度変更を行いません。読み込み待ちイベントでも相手の動画を自動で停止・再起動しません。残ったずれは下の矢印、「1拍目」、または「トラック」で手動修正します。

軽量化では音声をAACで保持し、変換時に先頭時刻を自動で詰めない設定にしました。音声が失われた場合や開始位置・長さがずれた場合は適用せず元動画を保ちます。元々無音の動画やカメラ録画に音声を追加するものではありません。

## 動画ファイル同士の再生開始

利用者が再生を押したときに、両方を無音で準備し、指定位置のコマが読めるまで待ってから再生を要求します。準備中は「開始を中止」でキャンセルできます。時計の差をなくそうとして開始を何度もやり直す処理はありません。

トラックのドラッグやシークは停止した状態で行います。矢印は再生中でも主役の時計を保って調整できます。利用者が設定した区間ループでは、指定した区間だけを繰り返します。自分の動画が始まる前の区間から再生した場合は、その開始位置に達した時点で自分の動画を一度だけ開始します。インカメとYouTube埋め込みはファイル2本用の「1拍目」画面の対象外です。YouTubeは既存の各動画設定で拍を合わせますが、デコード完了を直接確認できずフレーム単位の精度には制約があります。

読み込み遅延の模擬テストに加え、Chromeの実動画再生で、時計の差と連続する読み込み待ちイベントを与えても自動のpause・play・currentTime変更が発生しないこと、手動で指定した2点の保存と開始位置を確認しています。iPhone実機の画面・音声出力の遅延を計測した値ではありません。

YouTubeの重ね表示は、Chromeで実際の埋め込み動画とローカル動画の同時再生、比較／重ねる切替、YouTube操作用の一時非表示、手動の濃さ・位置・倍率変更を確認しています。390×844・844×390・375×667・667×375で2つの表示領域の一致とページ全体のはみ出しがないことも確認しました。模擬プレイヤーでは切替時のプレイヤー再生成・pause・play・seek要求が増えないことを検証しています。iPhone実機の確認ではありません。

通常再生の拍音と8カウントは「曲」または「速度・音」で選んだ音源側の実際の再生時刻を基準にします。音の出ていない側が読み込み待ちになっても拍音の予約は取り消しません。各動画の拍音プレビューは、その動画自身が基準です。手入力・解析候補・曲ライブラリから採用したBPMを小数1桁に丸めず保存します。最初だけ合って後半で徐々にずれる場合は、設定BPMと実際の曲のテンポが違う可能性があり、開始位置の移動だけでは解消できません。

動画を選ぶ画面は「選ぶ」「YouTube URL入力／インカメ」「履歴」の3段です。2026年9月のChrome・公式埋め込みの実測では、1.05・0.95倍は候補一覧になくても再生前・再生中とも反映されました。一方0.95238095倍は実際に0.95倍へ丸められました。例えばお手本126 BPM・自分120 BPMでは、YouTube 0.95倍・自分0.9975倍で両方119.7 BPMになります。お手本120 BPM・自分126 BPMならYouTube 1.05倍・自分1倍にできます。公式APIは指定値の反映を保証しないため、返された実倍率を確認して採用します。[YouTube IFrame API・速度指定](https://developers.google.com/youtube/iframe_api_reference#Playback_rate)


## トラック上の曲・タイミング・倍率

2本のトラック、シークバー、再生操作の配置を維持し、トラック上部の1行を「曲 / お手本 / 自分 / ←−0.001 / +0.001→ / ×1（選択）」にしています。トラックは練習画面に常時表示します。YouTube・比較／重ねる・動画選択前でも同じ操作ができ、横画面ではトラックと再生操作を左右に並べます。

- 選んだ曲を主役にし、矢印は反対側だけを調整します。お手本の曲なら矢印は自分の紫、自分の曲ならお手本の緑になります。
- ←は相手を早く、→は遅くします。1回で相手の動画時刻と開始位置を設定したずらし量だけ動かし、自動保存します。再生中の矢印では主役を停止・頭出ししません。YouTubeの古い時刻応答でも連打分を取りこぼさない処理を維持しています。
- 押した矢印が点灯し、操作回数と累計移動量をトラック上に一時表示します。範囲外なら橙色で「変更なし」と示します。押し外しでは数値は変わりません。
- 倍率の基本候補は0.25〜2.5倍の10段階です。YouTubeにも同じ候補を出し、再生開始時に実際に使える倍率を確認します。細かい設定や自動調整で選ばれた値は現在値として追加表示し、勝手に0.25刻みに丸めません。「速度・音」の詳細入力も戻しています。
- 「速度・音」の「矢印1回のずらし量」で0.0005・0.001（初期値）・0.005・0.01・0.05・0.1秒を選べます。この端末に保存され、常設トラックの矢印表示・操作・キーボード操作に反映されます。
- ずらし量は設定値の刻みです。動画のフレーム間隔やYouTubeの頭出し精度により、1回では映像が変わらない場合があります。トラックのドラッグも変更を自動保存します。

検証: 112件の自動テストに加え、Chromeの実動画で0.001秒・0.0005秒の連打、ずらし量の変更・再読み込み後の復元、主役の固定、色の切替、0.25刻みの全候補、2.5倍の適用、詳細倍率、保存、押し外しを確認しています。320×568・375×667・390×844・667×375・844×390で2本のトラックと上部1行の収まりを確認しています。YouTubeの実再生中も比較／重ねると縦横を切り替えてトラックが表示され続けること、主役を止めずに矢印を操作できることを確認しています。iPhone実機の確認ではありません。
